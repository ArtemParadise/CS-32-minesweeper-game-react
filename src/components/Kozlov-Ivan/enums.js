export const CellState = Object.freeze({
    CLOSED: "closed",
    OPENED: "opened",
    FLAGGED: "flagged",
});

export const GameStatus = Object.freeze({
    IN_PROGRESS: "in_progress",
    WON: "won",
    LOST: "lost",
});